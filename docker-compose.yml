version: '3.8'
services:
  backend:
    build: .
    ports:
      - '3000:3000'
    environment:
      - NODE_ENV=production
  frontend:
    image: mern-frontend
    ports:
      - '80:80'
    depends_on:
      - backend